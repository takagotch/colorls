### colorls
---
https://github.com/athityakumar/colorls

```
colorls
colorls -l
colorls -o
colorls -A
colorls -d
colorls -h
colorls -l
colorls --report
colorls -t spec
git stauts --short
colorls --gs spec
colorls --sd
colorls --sf
colorls --gs spec -t
colorls --gs spec -l
colorls -lA --sd

alias lc='colorls -lA --sd'
subl $(dirnaem $(gem which colorls))/yaml

cp $(dirname $(gem which colorls))/yaml/dark_colors.yaml ~/.config/colorls/dark_colors.yml`

untracked: yellow

cp $(dirname $(gem which colorls))/yaml/files.yaml ~/config/colorls/files.yml

swift: "\uF179"

gem update colorls
gem uninstall colorls

```


```
```

```
```
